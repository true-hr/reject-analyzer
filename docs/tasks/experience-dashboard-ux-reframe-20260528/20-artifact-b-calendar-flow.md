# 산출물 B 상세 정의: 캘린더 → 경험 흐름 캘린더

작성 예정.

## 현재 기본 방향

- 캘린더는 단순 기록 빈도 확인이 아니라 경험 축적 흐름을 보여주는 화면이어야 한다.
- `기록 없음`, `0건`, `~이 없습니다` 반복을 줄인다.
- 선택 날짜 상세는 `경험 흐름 / 분석 신호 / 다음 행동` 중심으로 재구성한다.
- DB schema 변경 없이 `HomeDashboard.jsx`와 기존 calendar utils를 활용한다.
