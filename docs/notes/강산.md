You are working on the PASSMAP project.

[PASSMAP PATCH RULES]
- UTF-8 유지
- 최소 수정만 수행
- 기존 구조 / 함수 / 순서 / 주석 유지
- 리팩토링 금지
- 새 파일 생성 금지
- 반드시 1파일만 수정
- upstream producer / viewmodel / scoring / gate / ranking / schema 수정 금지
- 이번 작업은 consumer render/UI patch only
- 최종 답변은 아래 형식만:
  1) PATCH CLASSIFICATION
  2) FILES MODIFIED
  3) PRECHECK
  4) EXACT ANCHORS
  5) BEFORE / AFTER
  6) VALIDATION CHECKLIST

PATCH CLASSIFICATION:
SAFE PATCH — consumer-only UI fix

FILES MODIFIED:
- src/components/report/ReportV2Container.jsx

HARD LOCK:
- src/lib/simulation/buildSimulationViewModel.js 수정 금지
- src/App.jsx 수정 금지
- 어떤 upstream producer도 수정 금지
- 이번 요청은 ReportV2Container consumer render 문제다
- 화면에서 안 보이게 / 덜 겹치게 / 덜 반복되게 만드는 UI patch만 수행

USER-REPORTED ISSUES TO FIX:
1. 초록색 "근거 있음" 배지가 없어졌으면 좋겠음
2. [리스크 발생 원인] 과 [채용 공고 vs 내 이력서 비교] 간격이 너무 가까움
3. Hero의 아래 메타라인 제거
   - 리스크 분류: ...
   - 확인 상태: ...
   - 출처: ...
4. [역할 판단] 카드에서 소제목과 설명이 사실상 동일함
   - 설명을 보강하거나, 최소한 headline 반복이 아니게 바꿔야 함

IMPORTANT:
The previous upstream change to buildSimulationViewModel.js does NOT satisfy this task.
Do not touch headline producer logic in this patch.
Fix only the actual consumer render in ReportV2Container.jsx.

PRECHECK:
Find actual existing locations first around:
- green evidence/status badge render path
- hero top card render block
- hero meta row render block
- type/role card render block
- section stack spacing between "리스크 발생 원인" and "채용 공고 vs 내 이력서 비교"
- visible text / fallback / dedupe helper cluster

TASK A — Remove green "근거 있음" badge
Requirements:
- Remove the visible green "근거 있음" badge from all currently rendered cards in this screen
- This includes Hero / 역할 판단 / 리스크 발생 원인 and any same-path repeated render
- Remove render only; keep data intact
- Do not remove other non-target chips unless they are literally the same evidence-present green badge
- After removal, no empty badge slot / dead margin / broken alignment should remain

TASK B — Increase spacing between:
- [리스크 발생 원인]
- [채용 공고 vs 내 이력서 비교]
Requirements:
- Only widen this specific vertical gap
- Do not globally change whole page spacing contract
- Use local wrapper margin/padding/gap adjustment only
- Result should visibly separate the two sections

TASK C — Remove Hero bottom meta row
Remove this render block from Hero:
- 리스크 분류: ...
- 확인 상태: ...
- 출처: ...
Requirements:
- Keep hero title / description / highlight box
- Remove only the bottom metadata row
- Clean up spacing after removal so the card does not look broken

TASK D — Fix duplicated body in [역할 판단]
Current problem:
- headline: 역할 범위는 팀 범위 쪽으로 읽힙니다
- body: 역할 범위는 팀 범위 쪽으로 읽힙니다.
- body repeats headline and adds no new information

Requirements:
- Keep headline if needed
- body must not repeat headline
- Prefer existing context/support/explanation text if available
- If existing candidates are duplicate/generic, synthesize one short supporting sentence
- body should explain one of:
  - 왜 그렇게 읽히는지
  - 어디까지는 읽히고 어디가 부족한지
  - 무엇을 더 보여주면 해석이 강화되는지

Allowed last-resort fallback examples:
- 현재 문서에서는 개인 단위 성과보다 팀 단위 기여가 먼저 읽혀 역할 폭이 보수적으로 해석될 수 있습니다.
- 담당 범위는 보이지만, 직접 주도한 범위가 어디까지였는지는 한 번 더 확인될 수 있습니다.
- 역할 방향은 맞지만, 책임 범위와 주도 수준을 더 또렷하게 보여주면 해석이 강화될 수 있습니다.

Do not hardcode blindly.
Use them only if existing body candidates are missing or near-duplicate of headline.

DEDUP CONTRACT:
Add or reuse a tiny helper that:
- normalizes visible text
- compares headline/body similarity
- rejects exact or near-duplicate body text
- rejects raw/internal/judgment-like filler

DO NOT:
- change ordering
- change risk ranking
- change comparison table content
- change hero producer logic
- revive hidden sections
- add new sections/cards
- modify upstream data contracts

SUCCESS CRITERIA:
1. "근거 있음" green badge no longer appears in the visible screen
2. Hero bottom metadata row is gone
3. Gap between "리스크 발생 원인" and "채용 공고 vs 내 이력서 비교" is visibly larger
4. 역할 판단 body is no longer identical to the headline
5. No raw/internal/dev language appears
6. No other layout contracts are broken

OUTPUT FORMAT:
Return only:
1) PATCH CLASSIFICATION
2) FILES MODIFIED
3) PRECHECK
4) EXACT ANCHORS
5) BEFORE / AFTER
6) VALIDATION CHECKLIST